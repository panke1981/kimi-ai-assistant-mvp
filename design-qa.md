# Design QA

final result: passed

## Current Direction

数智经营 MVP 已从多页面原型整理为电脑端单屏「经营指挥中心」。用户在一个工作台内看数据、分析风险、追问 AI 策略分析师，并通过浮窗完成上传、字段确认、数据质量、模型设置和报告生成。

## Checked

- `/login` 可进入本地单用户工作台。
- `/`、`/files`、`/fields`、`/analysis`、`/assistant`、`/settings` 都落到同一个 `CommandCenter`，不再维护旧多页面主流程。
- 左侧导航、顶部工作区标签、命令栏和浮窗状态保持同步。
- `⌘K / Ctrl+K` 命令栏可打开字段确认、数据质量、设置和经营复盘报告。
- 上传、设置、报告浮窗支持 `Esc` 和遮罩关闭。
- 字段确认浮窗支持确认/忽略字段，统计会即时更新；本地单用户模式提示临时变更不保存。
- 设置中心浮窗内可直接填写模型接入配置；本地单用户模式下表单可见但不保存。
- 右侧“生成任务草稿”会进入执行跟踪，顶部计数、执行画布、互动记录同步变化。
- 1280px 宽度下显示完整单屏工作台且整页不滚动；1100px 宽度下显示电脑端大屏提示，不再出现手机导航。
- 浏览器烟测无控制台 error。
- `npm run check`、`npm run lint`、`npm run test`、`npm run build` 全部通过。

## Notes

- 当前设计验收以电脑端大屏为准，最小工作区宽度为 1180px，不做手机适配。
- 后续生产化重点：真实数据链路长测、API Key 加密存储、首屏性能 profiling。
- 交接状态和烟测路径见 `HANDOFF.md`。
